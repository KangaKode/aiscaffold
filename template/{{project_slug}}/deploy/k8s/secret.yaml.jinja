# {{ project_name }} - Kubernetes Secret
# Generated by aiscaffold v3
#
# SECURITY: Do NOT commit real secrets to version control.
# Use one of these approaches instead:
#   1. kubectl create secret generic {{ project_slug }}-secrets \
#        --from-literal=API_KEY='your-key' \
#        --dry-run=client -o yaml | kubectl apply -f -
#   2. Use sealed-secrets or external-secrets operator
#   3. Inject via CI/CD pipeline

apiVersion: v1
kind: Secret
metadata:
  name: {{ project_slug }}-secrets
  labels:
    app: {{ project_slug }}
type: Opaque
stringData:
  API_KEY: "CHANGE-ME-IN-PRODUCTION"
{% if persistence == 'postgres' %}
  DATABASE_URL: "postgresql://{{ project_slug }}:CHANGE-ME@{{ project_slug }}-db:5432/{{ project_slug }}"
{% endif %}
