# Agentic Governance

How the scaffold governs autonomous agent behavior: who approves what, what gets recorded, what data is protected, and where the money stops. This document maps each governance domain to the code that implements it and the tests that prove it -- and, just as deliberately, states what the scaffold does **not** claim to do.

---

## Capability Matrix

| Domain | What it does | Implementation | Tests |
|--------|--------------|----------------|-------|
| Premise refusal | Agents may collectively refuse a flawed task before any expensive phase: each agent gets one cheap premise check, and enough independent refusals (threshold clamped so one agent can never veto alone) short-circuit the run with what is wrong, what is missing, and a better question. Fails open per agent; skipped without an LLM (Sentinel remains the fail-closed backstop) | `orchestration/premise.py`, `orchestration/round_table.py` | `tests/test_premise_gate.py` |
| Human oversight | Graduated autonomy levels (1 = trusted, 6 = most restricted) map to approval gates, specialist caps, rate multipliers, and conflict auto-escalation; restricted levels force a human check-in before results are acted on | `orchestration/autonomy.py`, `orchestration/round_table_helpers.py` (`apply_approval_gate`), `orchestration/chat_orchestrator.py`, `learning/checkin_manager.py` | `tests/test_governance.py` |
| Audit trail | Metadata-only deliberation timeline (phases, agent counts, durations, outcomes) keyed by correlation id; detail values are structurally limited so prompt/response content can never be stored. Each completed run stores a reasoning-chain SHA-256 over its ordered phase artifacts, so after-the-fact edits to the recorded phases are detectable | `orchestration/deliberation_audit.py`, `security/reasoning_chain_hash.py`, `api/routes/audit.py`, `learning/store.py` (`audit_events` table) | `tests/test_governance.py`, `tests/test_tamper_evidence.py` |
| Output attestation | Synthesized outputs can be signed (HMAC-SHA256) and verified, so a consumer sharing the key can confirm an output came from this platform unaltered | `enforcement/signer.py` | `tests/test_tamper_evidence.py` |
| Adversarial verification | A red-team harness of 6 deterministic hostile agents (no LLM, zero cost) implements the agent protocol and attacks a full round table with injection payloads in every field, asserting the defenses contain them end-to-end | `tests/adversarial_agents.py`, `tests/adversarial_payloads.py`, `tests/test_adversarial_defense.py` | `tests/test_adversarial_defense.py` |
| Sequence monitoring | Backward-chaining detection of multi-step extraction playbooks (e.g. query -> pull corrections -> export) that stay under volume thresholds; matches persist as integrity flags | `harness/sequence_detector.py` | `tests/test_tamper_evidence.py` |
| Data protection | PII redaction (emails, SSNs, Luhn-verified card numbers, IPs, phones, names-in-context) with Unicode normalization against homoglyph/invisible-char obfuscation, applied before correction text is persisted | `security/pii.py`, `learning/corrections.py` | `tests/test_governance.py` |
| Cost control | Per-tenant LLM budgets with warn/exhaust thresholds; exhausted tenants are blocked before the provider call; spend persists across restarts when a store is configured | `llm/budget_manager.py`, `llm/client.py`, `api/routes/budgets.py`, `learning/store.py` (`budget_spend` table) | `tests/test_governance.py` |
| Behavioral monitoring | Agent behavioral baselines, vote-lockstep/collusion detection, correction drift, and user activity thresholds; findings persist as integrity flags for human review | `learning/activity.py`, `learning/collusion.py`, `learning/override_detector.py`, `api/routes/activity.py` | `tests/test_learning_store.py` |
| Access control | Agent identity tokens verified at dispatch, per-agent rate limits, scope filtering of task context, suspension and credential lifecycle | `agents/identity.py`, `agents/rate_limiter.py`, `orchestration/scope_filter.py`, `agents/registry.py` | `tests/test_agent_identity.py` |
| Knowledge integrity | Content policy screens knowledge writes for standing-rule manipulation ("always classify X as safe", "don't log this"); rejected writes are refused and recorded as integrity flags; corrections require four-eyes human approval, with the whole lifecycle exposed API-first at `/api/v1/corrections` | `learning/content_policy.py`, `learning/corrections.py`, `learning/override_detector.py`, `api/routes/corrections.py` | `tests/test_governance.py`, `tests/test_learning_store.py`, `tests/test_corrections_api.py` |
| Right to be forgotten | GDPR Art. 17 hard-delete of learned corrections (actual row removal, not soft-delete), per-tenant daily cap against bulk-wipe abuse, metadata-only audit event per erasure | `learning/erasure.py`, `api/routes/corrections.py` (`DELETE`) | `tests/test_corrections_api.py` |
| Process learning | Deterministic reflections after each round table (agent effectiveness, evidence patterns, challenge impact, dissent value) from structured fields only -- no LLM, capped per session and per day, readable at `GET /api/v1/reflections` | `learning/reflector.py`, `api/routes/reflections.py` (`reflections` table) | `tests/test_learning_maturity.py` |
| Knowledge grounding | Approved corrections and error schemas ground all three resolution tiers: single-shot answers, chat synthesis context, and round-table task context (injected as wrapped untrusted content, scope-filtered per agent) | `learning/knowledge_context.py`, `orchestration/single_shot.py`, `api/routes/chat.py`, `api/routes/round_table.py` | `tests/test_learning_wiring.py` |
| Knowledge compaction | Clusters of approved corrections generalize into reusable error schemas (deterministic templating, >= 3 corrections from >= 2 distinct proposers), auto-extracted on each approval and served alongside raw corrections | `learning/error_schemata.py`, `api/routes/corrections.py` (`error_schemas` table) | `tests/test_learning_maturity.py`, `tests/test_learning_wiring.py` |
| Contradiction detection | Pairwise TF-IDF similarity + negation heuristic over a tenant's approved corrections flags pairs that likely disagree ("use X" vs "stop using X"); auto-scanned on each approval, findings persist as integrity flags for human review, never auto-retired | `learning/contradiction.py`, `api/routes/corrections.py` | `tests/test_learning_maturity.py`, `tests/test_learning_wiring.py` |
| Preference graduation | Stable preferences promote to a cross-project global profile only through the check-in flow: `/propose` opens pending check-ins, `/apply` refuses anything without an explicitly approved graduation check-in | `learning/graduation.py`, `api/routes/graduation.py` | `tests/test_learning_wiring.py` |

---

## Known Limitations / Non-Claims

Stating what a control does *not* do is part of the control. These are the honest boundaries of the current implementation:

- **Heuristic classifiers are not ML-grade.** The content policy and override detector are regex/keyword heuristics. They catch obvious manipulation phrasings and will miss paraphrases, other languages, and creative rewording. They reduce risk; they do not eliminate it.
- **In-memory rate limits and budgets-without-a-store reset on restart.** Agent rate limits are process-local sliding windows. Budget spend only survives restarts when a learning store is configured; without one, a restart is a budget reset.
- **The audit trail is tamper-evident within a run, not tamper-proof.** Each completed deliberation stores a reasoning-chain SHA-256 over its ordered phase artifacts, so editing a recorded phase and recomputing the hash will not match the stored digest -- casual after-the-fact edits are detectable. But the digest lives in the same database as the artifacts, so anyone who can alter the phases can also overwrite the digest; writes are still fire-and-forget (a failed write is logged, not raised). Likewise, output signing is symmetric (HMAC), so anyone who can verify can also sign -- it gives integrity and platform attestation for key-sharing consumers, not third-party non-repudiation. For that you need append-only storage, an external timestamp authority, and asymmetric signatures (swap the HMAC in `enforcement/signer.py` for Ed25519 -- same interface). See PLATFORM_GUIDE.md compliance section.
- **Erasure removes rows, not history.** `DELETE /api/v1/corrections/{id}` hard-deletes the live row, but database backups, WAL archives, and any downstream exports made before the erasure still contain the data. A complete Art. 17 program also needs backup rotation policy and downstream propagation -- deployment concerns outside the scaffold.
- **PII patterns are not a compliance guarantee.** Redaction covers common structured formats (emails, SSNs, card numbers, IPs, phones) plus a deliberately modest name heuristic. It will miss unstructured personal data, non-US formats, and context-dependent identifiers. It is a harm-reduction layer, not a GDPR/CCPA control by itself.
- **Single-node assumptions.** Tenant context uses process-local contextvars, autonomy env overrides are parsed once per process, and check-ins/budgets assume one coordinating instance unless you point everything at a shared store. Multi-node deployments need shared persistence and per-instance configuration discipline.

---

## Extension Points

- **SIEM export**: the `audit_events` table is a clean feed for a log shipper -- poll by `created_at` and forward to your SIEM.
- **Ground-truth verification**: plug a real `GroundTruthProvider` into the enforcement pipeline so numeric claims are checked against authoritative data.
- **Multi-provider model diversity**: model routing (`llm/model_router.py`, `MODEL_ROUTING_ENABLED`) already maps roles to cost tiers with cascade and budget-aware downgrades. Point the frontier tier (used by `sentinel_analysis`) at a different provider than your domain agents via `MODEL_TIER_MAP_JSON` so one provider's blind spots don't propagate.
- **Workload identity**: replace `issue_token`/`verify_token`/`hash_token` in `agents/identity.py` with your corporate STS or service-mesh identity system.
- **Real-time human escalation**: wire `CheckInManager` to a pager/chat integration so approval-required results notify a person immediately instead of waiting to be polled.
- **Per-call sandboxing**: execute agent-initiated tool calls inside a sandbox (container, seccomp, or WASM) so a manipulated agent's blast radius stays bounded.
